<h3>
    Hi! {{own.name}}
</h3>
<div>
    <ul class="user_list">
    </ul>
</div>
<script>
    const userId = '{{own.id}}';
    const userName = '{{own.name}}';
    connectRooms(userId, userName);
</script>
